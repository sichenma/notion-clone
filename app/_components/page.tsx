const componentsPage = () => {
    return ( 
        <div>
            Components page. It shouldn't be a useful path. Because the folder name is leading with underscore.
        </div> 
    );
}
 
export default componentsPage;